#!/usr/bin/env sh
set -e

npm run lint:fix
npm run format

if ! git diff --quiet; then
  echo ""
  echo "lint:fix / format changed files. Stage and commit them, then push again."
  git status --short
  exit 1
fi
